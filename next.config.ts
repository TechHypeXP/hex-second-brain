/** @type {import('next').NextConfig} */
const nextConfig = {
  // The output property is for the production build. 'standalone' is a good choice for Docker.
  output: 'standalone',
};

module.exports = nextConfig;